<div class="flex gap-2 items-center">
  <%= form_with model: link, data: { controller: 'reset-form', action: 'turbo:submit-end->reset-form#reset' } do |f| %>
    <%= f.text_field :url, placeholder: 'https://example.com', class: "flex-grow rounded", required: true, autofocus: true %>
    <%= f.button 'Save', class: "px-3 py-2 bg-gray-100 rounded" %>
  <% end %>
</div>
